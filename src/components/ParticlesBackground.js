import Particles from 'react-tsparticles'
import ParticlesConfig from './config/Paricles-config'
export const ParticlesBackground = () => {
  return (
    <Particles params = {ParticlesConfig} >
       
    </Particles>
  )
}
